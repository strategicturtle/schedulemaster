// ScheduleMaster Prisma schema
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// Real models (Schedule, Folder, Comment, Survey, …) will be added here as
// the app is built out. The mock Event model was removed.
